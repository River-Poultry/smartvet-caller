PORT=4500

# Secret used to sign user session tokens — change this in production
JWT_SECRET=smartvet-dev-secret-change-me

# Custom AI model endpoint (your trained weights served behind an API)
AI_MODEL_API_URL=https://your-ai-model-endpoint.example.com/v1/generate
AI_MODEL_API_KEY=replace-with-your-api-key
